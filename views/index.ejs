<!DOCTYPE html>
<html>
  <head>
    <title>session</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body> 
    <main>
      
      <% if (locals.user) { %>
        <style>
          body{
            background-color: <%= locals.user.color %>;
          }
        </style>
        <h1>Hola <%= locals.user.name %> </h1>
        <p>Elegiste el color <%= locals.user.color %> </p>
        <p>Tu email es: <%= locals.user.email %> </p>
        <p>Y tu edad es: <%= locals.user.age =! "" ? locals.user.age : "No lo se" %> </p>
        <a href="/next">Siguiente</a>
        
     <% } else { %> 
      <form action="" method="POST">
        <label for="name">Ingrese su nombre</label>
        <input type="text" id="name" name="name">
        <span><%= locals.errors && errors.name ? errors.name.msg : null %> </span>
        <label for="color">Elija su color favorito</label>
        <input type="color" name="color" id="color">
        <span><%= locals.errors && errors.color ? errors.color.msg : null %> </span>
        <label for="email">Ingrese su email</label>
        <input type="email" name="email" id="email">
        <span><%= locals.errors && errors.email ? errors.email.msg : null %> </span>
        <label for="age">Ingrese su edad</label>
        <input type="number" name="age" id="age">
        <label for="recordar">Recordar color</label>
        <input type="checkbox" name="recordar" id="recordar">
        <button type="submit">Enviar</button>
      </form>
      <% } %>
    </main>
  </body>
</html>
